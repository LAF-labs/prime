# AGENTS.md — LAF Agent

The project instructions live in [CLAUDE.md](CLAUDE.md): stack, layout,
commands, conventions, build validation, and the accumulated engineering
learnings. Read that file.

This file used to be a second, drifting copy of it. Agents that discover
context files pick up both `AGENTS.md` and `CLAUDE.md`, so keeping two versions
meant feeding them duplicated — and eventually contradictory — instructions.
One document, one source of truth.
